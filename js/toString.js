//TO STRING

//----------------Number 0 to String
let value = 0;

//1 way String()
let toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------Number 1 to String
value = 1;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------Infinity to String
value = Infinity;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------NaN to String
value = NaN;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------Boolean (logic) TRUE to String
value = true;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------Boolean (logic) FALSE to String
value = false;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------undefined to String
value = undefined;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);

//----------------null to String
value = null;

//1 way String()
toString = String(value);
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК з допомогою конструктора String() -- `, typeof toString);

//2 way concatenation
toString = value + "";
console.log(`Значення змінної value`, value);
console.log(`Значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, toString);
console.log(`Тип даних значення ${value}, змінної value,
приведене в тип РЯДОК через конкатенацію -- `, typeof toString);
